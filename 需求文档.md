下面是一份：

# 企业级 AI RAG 知识库系统（V1 基础版）

的完整需求文档。

这份文档的目标：

```text
用于指导 AI 后续持续开发
并作为整个项目长期演进的基础架构文档
```

它不是 Demo 文档。

而是：

# 面向未来扩展、多租户、多企业、多部署场景

的 AI Infra 级设计。

---

# 企业级 AI RAG 知识库系统（V1）

## 1. 项目目标

构建一套：

# 企业级 AI RAG 知识库系统

系统具备：

* 多文档上传
* 向量检索
* AI 问答
* Tool Calling
* 多轮对话
* Memory
* 企业级扩展能力

系统需支持：

* 多企业
* 多知识库
* 多部署环境
* 后续无限扩展

并满足：

* 高性能
* 高可维护性
* 高扩展性
* 企业级工程规范

---

# 2. 技术栈

## Frontend

```text
Vue3
Vite
TypeScript
Pinia
Element Plus / Naive UI
Cloudflare Pages
```

---

## Backend

```text
Python
FastAPI
Docker
PostgreSQL
pgvector
SQLAlchemy / SQLModel
httpx
pydantic-settings
```

---

## AI

```text
LongCat Chat Model
字节/火山 Embedding
RAG
Tool Calling
Memory
```

---

# 3. 系统定位

系统定位：

# AI Knowledge Platform

不是简单聊天系统。

而是：

# 企业 AI 基础设施平台

后续需支持：

* 企业知识库
* 企业 Agent
* 企业 AI 工作流
* AI 自动化
* 多模型
* 多向量库
* 多 Tool 系统

---

# 4. 核心功能（V1）

## 4.1 用户系统（预留）

V1 可先不实现登录。

但数据库结构必须预留：

```text
user_id
workspace_id
tenant_id
```

避免后续重构。

---

# 4.2 PDF 文档上传

支持：

```text
PDF 上传
```

后续扩展：

```text
DOCX
TXT
Markdown
Excel
PPT
```

---

# 4.3 文档解析

当前：

```text
PyMuPDF
```

功能：

```text
PDF → Text
```

后续扩展：

```text
OCR
多模态图片解析
表格识别
```

---

# 4.4 Chunk 切片

支持：

```text
chunk_size
chunk_overlap
```

需配置化。

默认：

```python
chunk_size = 500
chunk_overlap = 100
```

---

# 4.5 Embedding

Embedding 使用：

```text
字节/火山 Embedding
```

要求：

* embedding provider 可替换
* 模型配置化
* 后续支持 OpenAI / BGE / Jina

---

# 4.6 向量存储

数据库：

```text
PostgreSQL + pgvector
```

要求：

* 支持向量检索
* 支持 metadata
* 支持 file_id
* 支持 workspace_id

---

# 4.7 RAG 检索

流程：

```text
用户问题
↓
embedding
↓
pgvector 检索
↓
返回相关 chunk
↓
拼 Prompt
↓
LLM
```

---

# 4.8 AI Chat

模型：

```text
LongCat
```

要求：

* 支持 streaming
* 支持 markdown
* 支持多轮对话
* 支持 memory
* 支持 tool calling

---

# 4.9 Tool Calling

V1 必须支持：

```text
天气 API Tool
```

架构必须支持：

```text
Tool Registry
```

后续扩展：

* SQL Tool
* Search Tool
* RAG Tool
* Email Tool
* GitHub Tool

---

# 4.10 Memory

V1：

```text
Short-term memory
```

支持：

```text
messages 截断
```

后续：

```text
Summary memory
RAG memory
```

---

# 5. 系统架构

## 整体架构

```text
Vue3 Frontend
↓
FastAPI Backend
↓
PostgreSQL + pgvector
↓
Embedding Service
↓
LongCat LLM
```

---

# 6. 前端架构

## 技术要求

```text
Vue3 + TypeScript
```

必须：

* Composition API
* 模块化
* Pinia 状态管理
* API 封装
* Markdown 渲染
* Streaming 支持

---

## 页面结构

### V1 页面

```text
/chat
/upload
/files
/settings
```

---

## Chat 页面

功能：

* 多轮对话
* markdown 渲染
* streaming 打字机效果
* 自动滚动
* Tool 调用展示
* 错误处理

---

## Upload 页面

功能：

* PDF 上传
* 上传进度
* 上传状态
* 重复文件检测

---

# 7. 后端架构

## FastAPI 分层

必须：

```text
routers
services
repositories
models
schemas
core
utils
```

禁止：

```text
单文件项目
```

---

## 推荐目录结构

```text
backend/

app/
├── api/
├── core/
├── db/
├── models/
├── repositories/
├── services/
├── schemas/
├── tools/
├── rag/
├── memory/
├── prompts/
├── utils/
└── main.py
```

---

# 8. 数据库设计

## files 表

```sql
files
```

字段：

```text
id
workspace_id
file_name
file_hash
status
created_at
```

---

## documents 表

```sql
documents
```

字段：

```text
id
file_id
chunk_index
content
embedding
metadata
created_at
```

---

## chats 表（预留）

```text
chat_sessions
chat_messages
```

---

# 9. 文件去重

必须：

```text
文件 md5 去重
```

避免：

* 重复 embedding
* 重复存储

---

# 10. Docker 部署

## Backend

使用：

```text
Docker Compose
```

服务：

```text
backend
postgres
pgvector
redis（预留）
```

---

## Frontend

部署：

```text
Cloudflare Pages
```

自动：

```text
GitHub Actions CI/CD
```

---

# 11. 环境配置

必须：

```text
pydantic-settings
```

禁止：

```text
硬编码 API Key
```

---

# 12. Prompt Engineering

必须：

* System Prompt 分离
* Prompt 模板化
* Markdown 输出规范
* Structured Output 预留

---

# 13. Markdown 输出规范

AI 输出必须：

* 标准 Markdown
* 合法代码块
* 合法表格
* 不允许 HTML

---

# 14. Streaming

必须支持：

```text
stream=True
```

前端：

* 打字机效果
* Markdown streaming 渲染

---

# 15. Tool Calling 架构

必须：

```text
Tool Registry
```

支持：

```python
register_tool()
execute_tool()
```

---

# 16. RAG 架构要求

必须支持：

```text
多文件
多知识库
多企业
```

禁止：

```text
所有 chunk 混在一起
```

---

# 17. 安全要求

必须考虑：

## Prompt Injection

禁止：

* 任意 SQL
* 任意 Tool 调用
* 任意系统 Prompt 泄露

---

## API 安全

必须：

* 限流
* CORS
* Token 验证（预留）

---

# 18. 日志系统

必须：

```text
结构化日志
```

记录：

* API 请求
* Tool 调用
* RAG 检索
* LLM 请求耗时

---

# 19. 错误处理

必须：

* 全局异常处理
* 统一响应格式
* 用户友好错误

---

# 20. AI 开发规范

AI 后续生成代码时必须遵守：

## 必须

* 类型注解
* 分层架构
* 可维护
* 可扩展
* 企业级代码规范

---

## 禁止

* 单文件堆叠
* 全局变量污染
* 硬编码
* Demo 风格代码

---

# 21. 后续扩展方向

## V2

* 用户系统
* 多 Workspace
* OCR
* 图片理解
* 多模型切换
* 多向量库
* Redis Cache

---

## V3

* Agent
* Workflow
* Summary Memory
* RAG Memory
* MCP
* Auto Tool Planning

---

## V4

* SaaS 化
* 多租户
* 企业权限
* Kubernetes
* 分布式向量检索

---

# 22. 项目目标（长期）

最终目标：

构建：

# 企业级 AI Knowledge OS

支持：

* 企业知识库
* 企业 AI Agent
* 企业 Workflow
* 企业 AI 自动化

并支持：

```text
无限扩展
无限 Tool
无限知识库
无限企业部署
```

---

# 23. 当前阶段目标（V1）

当前 V1 目标：

# “最小可运行企业级 RAG 平台”

必须优先保证：

* 架构正确
* 可扩展
* 工程规范

而不是：

```text
快速堆功能
```
